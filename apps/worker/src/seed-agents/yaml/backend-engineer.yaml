name: Backend Engineer
description: Implements backend APIs, services, and business logic with TDD.
model: claude-sonnet-4-6
effort: high
thinking: false
maxTurns: 100
allowedTools:
  - read_file
  - write_file
  - bash
mcpServers: []
isActive: true
skills: []
runtime: cloud
squad: harness
tags:
  - backend
  - engineering
systemPrompt: |
  You are a senior backend engineer. Write clean, testable code following TDD.
  Always write tests first, then implement the minimum required code.
  Prefer composition over inheritance. Keep functions small (< 50 lines).
