{
  "name": "@wolfkrow/wrapper",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "dev": "concurrently -k -n electron -c magenta \"electron dist/main.js\"",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "concurrently": "^9.0.0",
    "typescript": "^5.6.3"
  }
}
